<UserControl
    x:Class="HappyWorkEveryday.MyUserContorls.MyBackUp"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HappyWorkEveryday.MyUserContorls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition Height="3*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>

        </Grid.ColumnDefinitions>

        <ComboBox x:Name="cbalias" Grid.Row="0" Grid.Column="0" ItemsSource="{Binding alias,UpdateSourceTrigger=PropertyChanged}">

        </ComboBox>

        <ComboBox x:Name="cbtechnology" Grid.Row="0" Grid.Column="1" ItemsSource="{Binding technology,UpdateSourceTrigger=PropertyChanged}">

        </ComboBox>

        <StackPanel x:Name="stackpanel" Grid.Row="1" Grid.ColumnSpan="2">

        </StackPanel>

        <Button Content="Add" Click="btnAdd_Click" x:Name="btnAdd" Grid.Row="2"></Button>
    </Grid>
</UserControl>
